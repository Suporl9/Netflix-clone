import React from 'react'

function PrivateRoute({component: Component,...rest}) {
    const {currentuser} = useA
    return (
        <div>
            
        </div>
    )
}

export default PrivateRoute
